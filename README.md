## Truco AI

Este proyecto consiste en un bot de inteligencia artificial que puede jugar al truco, un juego de cartas muy popular en Argentina.

### Cómo funciona

El bot de truco está programado en Python y utiliza una combinación de algoritmos de búsqueda de árboles de juego y redes neuronales para tomar decisiones sobre qué jugadas hacer en cada turno del juego. El bot también es capaz de evaluar la fuerza de la mano del jugador y decidir cuándo jugar las cartas de envido.

### Requisitos

Para ejecutar el bot de truco, necesitas tener instalado Python 3 en tu ordenador. Además, el bot utiliza las bibliotecas TensorFlow y Keras para su componente de redes neuronales, por lo que debes instalarlas antes de ejecutar el bot.

### Cómo ejecutar

Para ejecutar el bot de truco, primero asegúrate de tener Python 3 y las bibliotecas TensorFlow y Keras instaladas. Luego, descarga el código fuente del proyecto y abre una terminal en la carpeta raíz del proyecto. Ejecuta el siguiente comando para instalar todas las dependencias:

```
pip install -r requirements.txt
```

Una vez instaladas las dependencias, ejecuta el siguiente comando para iniciar el bot:

```
python main.py
```

### Soporte

Este proyecto se ofrece "tal cual" sin garantías ni soporte. Si tienes preguntas o problemas, no dudes en crear un issue en el repositorio.

### Contribuir

Si deseas contribuir a este proyecto, ¡estamos abiertos a sugerencias! Puedes crear un pull request en el repositorio con tus cambios.

### Licencia

Este proyecto está licenciado bajo la licencia MIT. Consulta el archivo LICENSE para más detalles.

---

## Truco AI (English)

This project is an artificial intelligence bot that can play truco, a popular card game in Argentina.

### How it works

The truco bot is programmed in Python and uses a combination of game tree search algorithms and neural networks to make decisions about which plays to make on each turn of the game. The bot is also able to evaluate the strength of the player's hand and decide when to play envido cards.

### Requirements

To run the truco bot, you need to have Python 3 installed on your computer. Additionally, the bot uses the TensorFlow and Keras libraries for its neural network component, so you must install them before running the bot.

### How to run

To run the truco bot, first make sure you have Python 3 and the TensorFlow and Keras libraries installed. Then, download the source code of the project and open a terminal in the project root folder. Run the following command to install all dependencies:

```
pip install -r requirements.txt
```

Once dependencies are installed, run the following command to start the bot:

```
python main.py
```

### Support

This project is offered "as is" without warranties or support. If you have questions or issues, feel free to create an issue on the repository.

### Contributing

If you wish to contribute to this project, we are open to suggestions! You can create a pull request on the repository with your changes.

